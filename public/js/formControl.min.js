function show_data(ext) {
    ext = JSON.parse(ext);
    console.log($('#expiration-date').val().type());
    $('#data-id').val(ext.id);
    $('#size').val(ext.size);
    $('#type').val(ext.type);
    $('#expiration-date').val(ext.expiration_date);
    $('#location').val(ext.location);
    $('#place').val(ext.place);
    $('#floor').val(ext.floor);
    $('#observation').val(ext.observation);
}

